import { Title } from '@angular/platform-browser';

export interface Circuito {
    id?: number,
    nombre?: string,
    localidad?: string,
    pais?: string,
    longitud?: number,
    imagen?: string
};