import { FilterPilotoPipe } from './filter-piloto.pipe';

describe('FilterPilotoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPilotoPipe();
    expect(pipe).toBeTruthy();
  });
});
