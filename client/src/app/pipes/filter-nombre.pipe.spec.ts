import { FilterNombrePipe } from './filter-nombre.pipe';

describe('FilterNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
