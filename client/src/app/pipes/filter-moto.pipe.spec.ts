import { FilterMotoPipe } from './filter-moto.pipe';

describe('FilterMotoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMotoPipe();
    expect(pipe).toBeTruthy();
  });
});
